# Repo 타당성 검증 리포트

- **검증 대상**: `<repo 경로>`
- **검증일**: `<YYYY-MM-DD>`
- **Repo 타입**: **`<primary>`** (secondary: `<...>`) — 분류 근거: `<매칭 파일/패턴>`
- **기준 문서(intent baseline)**: `<채택한 문서 경로>`
- **실행한 검증 명령**: `<명령 목록>`
- **종합 판정**: **`<PASS | CONCERNS | FAIL>`** — `<한 줄 근거>`

## 검증 범위 (타입별 활성 축)

| 축 / 모듈 | 활성 | 점수 | 상태 |
|---|:--:|---:|---|
| A1 기획의도 정합성 | ✅ | `<0–100>` | `<pass/concern/fail>` |
| A2 AI-readable 구조 + 문서 완전성 | ✅ | `<0–100>` | `<...>` |
| A3 goal 검증 + 테스트 충분성 | ✅ | `<0–100>` | `<...>` |
| M-DATA 데이터/스키마 무결성 | `<✅/N/A>` | `<…>` | `<…>` |
| M-SEC 보안/시크릿 위생 | `<✅/N/A>` | | |
| M-DEP 의존성 건전성 | `<✅/N/A>` | | |
| M-CHANGE 변경 안전성/git 위생 | `<✅/N/A>` | | |
| M-API 계약 안정성 | `<✅/N/A>` | | |
| M-OPS 운영 준비성 | `<✅/N/A>` | | |
| M-PERF 성능 특성 | `<✅/N/A>` | | |

> N/A 축은 타입상 비활성 — 채점·판정에서 제외됨.

---

## A1 기획의도 정합성

**추적 매트릭스**

| 선언 기능/범위 | 상태 | 구현 증거 (path) |
|---|---|---|
| `<항목>` | `구현됨/부분구현/미구현/확인필요` | `<file:line>` 또는 `사용자 확인: <답변 요약>` |

**제약 준수 (Do-Not / 제외 항목 역검증)**

| 제약 | 준수 | 증거 (grep 결과) |
|---|---|---|
| `<예: No MCP>` | `O/X` | `<근거>` |

**미해소 의도 질문 (확인필요)** — 증거로 의도를 못 가른 항목. `--interview`로 재실행 시 1회 배치 질의로 확정됨.

| 모호점 | 왜 증거로 못 가르나 | 상태 |
|---|---|---|
| `<항목>` | `<증거 침묵/상충 근거>` | `확인필요` (또는 `해소됨 → <확정상태>`) |

> 핵심 범위에 `확인필요`가 1건+ 있으면 A1는 pass 불가(상한 concern). 확정하려면 `--interview` 플래그로 재실행.

발견사항: `<Critical/Major/Minor + 설명>`

---

## A2 AI-readable 구조 + 문서 완전성

| 항목 | 평가 | 증거 |
|---|---|---|
| 에이전트 가이드 | `<점수/근거>` | `<path>` |
| README 품질 | | |
| 구조 일관성 | | |
| 기계 판독 데이터 | | |
| 문서-코드 정합성 | | |
| 문서 완전성 | | |

발견사항: `<...>`

---

## A3 goal 검증 + 테스트 충분성

**실행 로그**

```
$ <명령>
<stdout 요약>
exit: <code>
```

> exit≠0은 **real failure** / **검증불가(blocked, 환경 시그니처)** 로 구분 표기한다. 위험 명령(`deploy`/`migrate`/`publish`/`push`/`seed` 등)은 `--interview` 미사용 시 "미실행(opt-in 필요)"으로 기록한다.

**테스트 충분성**: `<핵심 기능 커버 여부 / 커버리지 수치 / 형식적 테스트 여부>`

**성공지표 / DoD 매핑**

| 지표/DoD | 충족 | 증거 |
|---|---|---|
| `<항목>` | `O/X` | `<로그/경로>` |

발견사항: `<...>`

---

## 조건부 모듈 (활성화된 것만)

각 활성 모듈에 대해 작성. 비활성 모듈은 생략한다.
> 시크릿 증거는 **값을 마스킹**(예: `Tab…***`)하고 위치(`path:line`)만 적는다. 리포트 산출물 자체는 M-CHANGE 검사에서 제외한다.

### `<M-DATA / M-SEC / M-DEP / M-CHANGE / ...>`

| 항목 | 평가 | 증거 |
|---|---|---|
| `<항목>` | `<pass/concern/fail>` | `<grep/로그/path — 시크릿은 마스킹>` |

발견사항: `<Critical/Major/Minor + 설명>`

---

## 권고 액션 (우선순위순)

1. `[Critical]` `<...>`
2. `[Major]` `<...>`
3. `[Minor]` `<...>`
